# Alena files — deleted

`victoria.png`, `victoria_ref.jpg`, `alena.png`, `alena_ref.jpg` are gone.
Never restore. Never i2i.

Carousel does not i2i any host face, including `Виктория.png`.

If a prompt still says `viktoriaref.png`, `victoria-sheet.png`, `victoria.png`, or `Виктория.png` as i2i, drop the face ref.
