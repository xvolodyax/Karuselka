# Alena files — deleted

`victoria.png`, `victoria_ref.jpg`, `alena.png`, `alena_ref.jpg` are gone.
Never restore. Never i2i.

Only remaining Vika face file: `Виктория.png`
(repo: `carusel-memory/references/Виктория.png`, box: `/workspace/cover-refs/Виктория.png`).

If a prompt still says `viktoriaref.png`, `victoria-sheet.png`, or `victoria.png`, rewrite the lock to `Виктория.png` only.
